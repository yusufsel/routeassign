
spring:
  liquibase:
    enabled: false
  datasource:
    url: jdbc:h2:mem:test_db;DB_CLOSE_DELAY=-1
    username: sa
    password: sa
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        format_sql: true
        show_sql: false